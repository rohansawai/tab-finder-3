chrome.runtime.onInstalled.addListener(() => {
  console.log("Tab Finder extension installed")
})

// You could add functionality here to periodically index tab content
// for better searching capabilities, or to handle messages from the popup

